# Copy thành .env.local (dev) hoặc đặt trên Vercel (prod).
#
# DEV: để TRỐNG -> same-origin, Vite proxy /api & /socket.io về backend :8000.
# PROD (Vercel): điền URL backend Render, ví dụ:
#   VITE_BACKEND_URL=https://voicego-xxxx.onrender.com
VITE_BACKEND_URL=
